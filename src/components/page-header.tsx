export function PageHeader() {
  return (
    <header>
      <h1 className="text-3xl font-semibold tracking-tight text-balance">x402 vs MPP</h1>
      <p className="mt-2 text-sm text-muted-foreground text-pretty">
        Protocol comparison across transactions, volume, buyers, sellers, avg ticket, and top
        services by volume.
      </p>
    </header>
  );
}
